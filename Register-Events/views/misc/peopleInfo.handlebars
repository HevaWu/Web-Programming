<article class="people">
  <h1>{{ person.name }}</h1>

  {{#if person.events.length}}
  <h5>Events person will be attending</h5>
  <ul>
    {{#each person.events}}
    <li><a href="/events/{{this.id}}">{{this.title}}</a></li>
    {{/each}}
  </ul>
  {{/if}}

</article>
